#import "./Order.graphql"

query orders($limit: Int!, $after: Int!, $filter: FilterOrderInput, $orderBy: OrderByListInput) {
  orders(limit: $limit, after: $after, filter: $filter, orderBy: $orderBy) {
    totalCount
    edges {
      cursor
      node {
        ...OrderInfo
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
