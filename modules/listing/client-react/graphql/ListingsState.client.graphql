fragment ListingsStateInfo on ListingsState {
  orderBy {
    column
    order
  }
  filter {
    isActive
    priceRange {
      min
      max
    }
    weights {
      weight
    }
    categories {
      category
    }
    flavours {
      flavours
    }
    category
    searchText
  }
}
