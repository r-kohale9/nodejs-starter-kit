#import "./Review.graphql"

query allReviews(
  $userId: Int
  $limit: Int!
  $after: Int
  $orderBy: OrderByListInput
  $filter: FilterListInput
) {
  allReviews(
    userId: $userId
    limit: $limit
    after: $after
    orderBy: $orderBy
    filter: $filter
  ) {
    totalCount
    edges {
      cursor
      node {
        ...ReviewInfo
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
