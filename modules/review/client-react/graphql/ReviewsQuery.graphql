#import "./Review.graphql"

query reviews(
  $userId: Int
  $limit: Int!
  $after: Int
  $orderBy: OrderByListInput
  $filter: FilterListInput
) {
  reviews(
    userId: $userId
    limit: $limit
    after: $after
    orderBy: $orderBy
    filter: $filter
  ) {
    totalCount
    edges {
      cursor
      node {
        ...ReviewInfo
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}
